[project]
name="seed"
version="0.1.0"
requires-python=">=3.12"
dependencies=["typer","pydantic>=2","pyyaml","faker","pandas","rich","numpy"]
[project.scripts]
seed="seed.cli.main:app"
[build-system]
requires=["hatchling"]
build-backend="hatchling.build"
